# Commitments

Gold and input commitment files are intentionally absent until corresponding canonical artifacts exist.

Never write placeholder SHA-256 values. `gold_plaintext.sha256`, `gold_ciphertext.sha256`, and `inputs.sha256` are valid only when computed from the exact artifacts they commit to.
