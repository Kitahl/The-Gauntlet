# Session 3R / Test 3R — Space vs BASE

Certified three-session paired retest of the frozen Space / Research Discovery procedure.

The benchmark is generated from frozen first-party GitHub release metadata rather than public benchmark question/answer corpora. BASE and SPACE receive byte-identical question projections in independent sessions. The gold payload is gzip-compressed and base64-encoded so ordinary web search cannot surface semantic answer-key snippets.

Primary endpoint: exact release-fact retrieval plus first-party evidence recovery.
