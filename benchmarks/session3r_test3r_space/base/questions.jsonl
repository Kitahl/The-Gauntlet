{"id": "space_release_lookup_001", "prompt": "Find and verify the most recent stable GitHub Release for `pytest-dev/pytest` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_002", "prompt": "Find and verify the most recent stable GitHub Release for `fastapi/fastapi` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_003", "prompt": "Find and verify the most recent stable GitHub Release for `huggingface/datasets` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_004", "prompt": "Find and verify the most recent stable GitHub Release for `sequelize/sequelize` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_005", "prompt": "Find and verify the most recent stable GitHub Release for `streamlit/streamlit` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_006", "prompt": "Find and verify the most recent stable GitHub Release for `numpy/numpy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_007", "prompt": "Find and verify the most recent stable GitHub Release for `Textualize/rich` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_008", "prompt": "Find and verify the most recent stable GitHub Release for `vercel/next.js` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_009", "prompt": "Find and verify the most recent stable GitHub Release for `minio/minio` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_010", "prompt": "Find and verify the most recent stable GitHub Release for `starship/starship` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_011", "prompt": "Find and verify the most recent stable GitHub Release for `prometheus/prometheus` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_012", "prompt": "Find and verify the most recent stable GitHub Release for `envoyproxy/envoy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_013", "prompt": "Find and verify the most recent stable GitHub Release for `ggml-org/llama.cpp` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_014", "prompt": "Find and verify the most recent stable GitHub Release for `conda/conda` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_015", "prompt": "Find and verify the most recent stable GitHub Release for `BurntSushi/ripgrep` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_016", "prompt": "Find and verify the most recent stable GitHub Release for `denoland/deno` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_017", "prompt": "Find and verify the most recent stable GitHub Release for `neovim/neovim` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_018", "prompt": "Find and verify the most recent stable GitHub Release for `pandas-dev/pandas` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_019", "prompt": "Find and verify the most recent stable GitHub Release for `scipy/scipy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_020", "prompt": "Find and verify the most recent stable GitHub Release for `milvus-io/milvus` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_021", "prompt": "Find and verify the most recent stable GitHub Release for `opensearch-project/OpenSearch` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_022", "prompt": "Find and verify the most recent stable GitHub Release for `PrefectHQ/prefect` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_023", "prompt": "Find and verify the most recent stable GitHub Release for `crewAIInc/crewAI` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_024", "prompt": "Find and verify the most recent stable GitHub Release for `instructor-ai/instructor` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_025", "prompt": "Find and verify the most recent stable GitHub Release for `nodejs/node` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_026", "prompt": "Find and verify the most recent stable GitHub Release for `tensorflow/tensorflow` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_027", "prompt": "Find and verify the most recent stable GitHub Release for `psf/requests` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_028", "prompt": "Find and verify the most recent stable GitHub Release for `docker/compose` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_029", "prompt": "Find and verify the most recent stable GitHub Release for `apache/arrow` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_030", "prompt": "Find and verify the most recent stable GitHub Release for `ray-project/ray` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_031", "prompt": "Find and verify the most recent stable GitHub Release for `apache/airflow` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_032", "prompt": "Find and verify the most recent stable GitHub Release for `vitejs/vite` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_033", "prompt": "Find and verify the most recent stable GitHub Release for `openai/openai-node` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_034", "prompt": "Find and verify the most recent stable GitHub Release for `godotengine/godot` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_035", "prompt": "Find and verify the most recent stable GitHub Release for `pipxproject/pipx` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_036", "prompt": "Find and verify the most recent stable GitHub Release for `prisma/prisma` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_037", "prompt": "Find and verify the most recent stable GitHub Release for `openai/openai-python` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_038", "prompt": "Find and verify the most recent stable GitHub Release for `npm/cli` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_039", "prompt": "Find and verify the most recent stable GitHub Release for `fluxcd/flux2` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_040", "prompt": "Find and verify the most recent stable GitHub Release for `tmux/tmux` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_041", "prompt": "Find and verify the most recent stable GitHub Release for `yt-dlp/yt-dlp` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_042", "prompt": "Find and verify the most recent stable GitHub Release for `kubernetes/kubernetes` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_043", "prompt": "Find and verify the most recent stable GitHub Release for `fish-shell/fish-shell` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_044", "prompt": "Find and verify the most recent stable GitHub Release for `matplotlib/matplotlib` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_045", "prompt": "Find and verify the most recent stable GitHub Release for `pydantic/pydantic` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_046", "prompt": "Find and verify the most recent stable GitHub Release for `caddyserver/caddy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_047", "prompt": "Find and verify the most recent stable GitHub Release for `apache/beam` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_048", "prompt": "Find and verify the most recent stable GitHub Release for `nushell/nushell` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_049", "prompt": "Find and verify the most recent stable GitHub Release for `gradio-app/gradio` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_050", "prompt": "Find and verify the most recent stable GitHub Release for `celery/celery` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_compare_001", "prompt": "For `openssl/openssl` and `ollama/ollama`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_002", "prompt": "For `sqlalchemy/alembic` and `yt-dlp/yt-dlp`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_003", "prompt": "For `nodejs/node` and `getsentry/sentry`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_004", "prompt": "For `nushell/nushell` and `chroma-core/chroma`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_005", "prompt": "For `instructor-ai/instructor` and `BurntSushi/ripgrep`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_006", "prompt": "For `apache/beam` and `Textualize/rich`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_007", "prompt": "For `jupyterlab/jupyterlab` and `apache/arrow`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_008", "prompt": "For `helm/helm` and `nodejs/node`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_009", "prompt": "For `ansible/ansible` and `rancher/rancher`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_010", "prompt": "For `matplotlib/matplotlib` and `pallets/flask`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_011", "prompt": "For `scrapy/scrapy` and `apache/beam`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_012", "prompt": "For `docker/compose` and `helm/helm`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_013", "prompt": "For `fluxcd/flux2` and `docker/compose`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_014", "prompt": "For `microsoft/vscode` and `godotengine/godot`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_015", "prompt": "For `microsoft/semantic-kernel` and `milvus-io/milvus`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_016", "prompt": "For `prisma/prisma` and `istio/istio`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_017", "prompt": "For `typeorm/typeorm` and `mpv-player/mpv`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_018", "prompt": "For `elastic/kibana` and `prometheus/prometheus`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_019", "prompt": "For `apache/airflow` and `moby/moby`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_020", "prompt": "For `dagster-io/dagster` and `instructor-ai/instructor`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_021", "prompt": "For `caddyserver/caddy` and `sequelize/sequelize`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_022", "prompt": "For `duckdb/duckdb` and `etcd-io/etcd`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_023", "prompt": "For `microsoft/vscode` and `nodejs/node`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_024", "prompt": "For `openai/openai-python` and `vitejs/vite`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_025", "prompt": "For `huggingface/transformers` and `ollama/ollama`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_026", "prompt": "For `huggingface/transformers` and `numpy/numpy`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_027", "prompt": "For `pipxproject/pipx` and `openssl/openssl`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_028", "prompt": "For `astral-sh/ruff` and `pydantic/pydantic`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_029", "prompt": "For `vitejs/vite` and `caddyserver/caddy`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_030", "prompt": "For `sqlalchemy/alembic` and `scipy/scipy`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_predecessor_001", "prompt": "For `langchain-ai/langchain`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_002", "prompt": "For `envoyproxy/envoy`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_003", "prompt": "For `huggingface/datasets`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_004", "prompt": "For `ggml-org/llama.cpp`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_005", "prompt": "For `traefik/traefik`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_006", "prompt": "For `vllm-project/vllm`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_007", "prompt": "For `docker/buildx`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_008", "prompt": "For `scipy/scipy`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_009", "prompt": "For `prometheus/prometheus`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_010", "prompt": "For `junegunn/fzf`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_011", "prompt": "For `vuejs/core`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_012", "prompt": "For `nushell/nushell`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_013", "prompt": "For `fastapi/fastapi`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_014", "prompt": "For `weaviate/weaviate`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_015", "prompt": "For `facebook/react`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_016", "prompt": "For `minio/minio`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_017", "prompt": "For `pytest-dev/pytest`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_018", "prompt": "For `pallets/flask`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_019", "prompt": "For `mpv-player/mpv`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_020", "prompt": "For `yt-dlp/yt-dlp`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
