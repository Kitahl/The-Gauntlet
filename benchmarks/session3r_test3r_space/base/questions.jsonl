{"id": "space_release_lookup_001", "prompt": "Find and verify the most recent stable GitHub Release for `pallets/flask` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_002", "prompt": "Find and verify the most recent stable GitHub Release for `apache/superset` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_003", "prompt": "Find and verify the most recent stable GitHub Release for `vuejs/core` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_004", "prompt": "Find and verify the most recent stable GitHub Release for `python-poetry/poetry` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_005", "prompt": "Find and verify the most recent stable GitHub Release for `ClickHouse/ClickHouse` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_006", "prompt": "Find and verify the most recent stable GitHub Release for `scrapy/scrapy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_007", "prompt": "Find and verify the most recent stable GitHub Release for `pytorch/pytorch` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_008", "prompt": "Find and verify the most recent stable GitHub Release for `sveltejs/svelte` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_009", "prompt": "Find and verify the most recent stable GitHub Release for `k3s-io/k3s` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_010", "prompt": "Find and verify the most recent stable GitHub Release for `langchain-ai/langchain` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_011", "prompt": "Find and verify the most recent stable GitHub Release for `psf/black` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_012", "prompt": "Find and verify the most recent stable GitHub Release for `chroma-core/chroma` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_013", "prompt": "Find and verify the most recent stable GitHub Release for `helm/helm` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_014", "prompt": "Find and verify the most recent stable GitHub Release for `jupyter/notebook` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_015", "prompt": "Find and verify the most recent stable GitHub Release for `ansible/ansible` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_016", "prompt": "Find and verify the most recent stable GitHub Release for `moby/moby` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_017", "prompt": "Find and verify the most recent stable GitHub Release for `redis/redis` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_018", "prompt": "Find and verify the most recent stable GitHub Release for `junegunn/fzf` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_019", "prompt": "Find and verify the most recent stable GitHub Release for `dagster-io/dagster` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_020", "prompt": "Find and verify the most recent stable GitHub Release for `jupyterlab/jupyterlab` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_021", "prompt": "Find and verify the most recent stable GitHub Release for `etcd-io/etcd` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_022", "prompt": "Find and verify the most recent stable GitHub Release for `pallets/click` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_023", "prompt": "Find and verify the most recent stable GitHub Release for `fastapi/sqlmodel` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_024", "prompt": "Find and verify the most recent stable GitHub Release for `typeorm/typeorm` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_025", "prompt": "Find and verify the most recent stable GitHub Release for `astral-sh/ruff` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_026", "prompt": "Find and verify the most recent stable GitHub Release for `mpv-player/mpv` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_027", "prompt": "Find and verify the most recent stable GitHub Release for `openssl/openssl` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_028", "prompt": "Find and verify the most recent stable GitHub Release for `BerriAI/litellm` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_029", "prompt": "Find and verify the most recent stable GitHub Release for `hashicorp/terraform` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_030", "prompt": "Find and verify the most recent stable GitHub Release for `sqlalchemy/sqlalchemy` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_031", "prompt": "Find and verify the most recent stable GitHub Release for `microsoft/vscode` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_032", "prompt": "Find and verify the most recent stable GitHub Release for `weaviate/weaviate` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_033", "prompt": "Find and verify the most recent stable GitHub Release for `docker/buildx` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_034", "prompt": "Find and verify the most recent stable GitHub Release for `elastic/kibana` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_035", "prompt": "Find and verify the most recent stable GitHub Release for `cli/cli` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_036", "prompt": "Find and verify the most recent stable GitHub Release for `vllm-project/vllm` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_037", "prompt": "Find and verify the most recent stable GitHub Release for `grafana/grafana` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_038", "prompt": "Find and verify the most recent stable GitHub Release for `astral-sh/uv` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_039", "prompt": "Find and verify the most recent stable GitHub Release for `rancher/rancher` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_040", "prompt": "Find and verify the most recent stable GitHub Release for `huggingface/transformers` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_041", "prompt": "Find and verify the most recent stable GitHub Release for `pnpm/pnpm` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_042", "prompt": "Find and verify the most recent stable GitHub Release for `facebook/react` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_043", "prompt": "Find and verify the most recent stable GitHub Release for `duckdb/duckdb` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_044", "prompt": "Find and verify the most recent stable GitHub Release for `qdrant/qdrant` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_045", "prompt": "Find and verify the most recent stable GitHub Release for `istio/istio` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_046", "prompt": "Find and verify the most recent stable GitHub Release for `getsentry/sentry` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_047", "prompt": "Find and verify the most recent stable GitHub Release for `scikit-learn/scikit-learn` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_048", "prompt": "Find and verify the most recent stable GitHub Release for `curl/curl` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_049", "prompt": "Find and verify the most recent stable GitHub Release for `argoproj/argo-cd` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_lookup_050", "prompt": "Find and verify the most recent stable GitHub Release for `sqlalchemy/alembic` that was neither a draft nor a prerelease and was published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Also retain the first-party evidence URL you used in your prediction receipt.", "section": "primary_verified_release_retrieval", "task_type": "release_lookup"}
{"id": "space_release_compare_001", "prompt": "For `run-llama/llama_index` and `etcd-io/etcd`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_002", "prompt": "For `moby/moby` and `pydantic/pydantic`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_003", "prompt": "For `istio/istio` and `gradio-app/gradio`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_004", "prompt": "For `scikit-learn/scikit-learn` and `crewAIInc/crewAI`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_005", "prompt": "For `fastapi/fastapi` and `traefik/traefik`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_006", "prompt": "For `astral-sh/ruff` and `starship/starship`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_007", "prompt": "For `docker/compose` and `ray-project/ray`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_008", "prompt": "For `ClickHouse/ClickHouse` and `docker/compose`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_009", "prompt": "For `celery/celery` and `typeorm/typeorm`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_010", "prompt": "For `PrefectHQ/prefect` and `denoland/deno`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_011", "prompt": "For `ollama/ollama` and `celery/celery`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_012", "prompt": "For `BerriAI/litellm` and `istio/istio`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_013", "prompt": "For `typeorm/typeorm` and `docker/compose`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_014", "prompt": "For `godotengine/godot` and `instructor-ai/instructor`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_015", "prompt": "For `scikit-learn/scikit-learn` and `psf/requests`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_016", "prompt": "For `sharkdp/bat` and `prometheus/prometheus`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_017", "prompt": "For `pallets/click` and `crewAIInc/crewAI`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_018", "prompt": "For `argoproj/argo-cd` and `obsproject/obs-studio`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_019", "prompt": "For `traefik/traefik` and `typeorm/typeorm`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_020", "prompt": "For `jupyterlab/jupyterlab` and `milvus-io/milvus`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_021", "prompt": "For `ClickHouse/ClickHouse` and `argoproj/argo-cd`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_022", "prompt": "For `nushell/nushell` and `scikit-learn/scikit-learn`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_023", "prompt": "For `BurntSushi/ripgrep` and `fish-shell/fish-shell`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_024", "prompt": "For `streamlit/streamlit` and `vllm-project/vllm`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_025", "prompt": "For `prisma/prisma` and `caddyserver/caddy`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_026", "prompt": "For `huggingface/datasets` and `python-poetry/poetry`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_027", "prompt": "For `nushell/nushell` and `ray-project/ray`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_028", "prompt": "For `sqlalchemy/alembic` and `jupyter/notebook`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_029", "prompt": "For `prisma/prisma` and `huggingface/transformers`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_compare_030", "prompt": "For `qdrant/qdrant` and `openai/openai-python`, find and verify each repository's most recent stable GitHub Release published on or before 2026-08-24T23:59:59Z. Return exactly one JSON object with keys `later_repo`, `repo_a_tag`, `repo_a_date`, `repo_b_tag`, and `repo_b_date`. `later_repo` must be the repository whose qualifying release was published later. Retain first-party evidence URLs for both repositories in your prediction receipt.", "section": "secondary_multi_source_comparison", "task_type": "release_compare"}
{"id": "space_release_predecessor_001", "prompt": "For `containerd/containerd`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_002", "prompt": "For `conda/conda`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_003", "prompt": "For `python-poetry/poetry`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_004", "prompt": "For `run-llama/llama_index`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_005", "prompt": "For `vitejs/vite`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_006", "prompt": "For `matplotlib/matplotlib`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_007", "prompt": "For `ClickHouse/ClickHouse`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_008", "prompt": "For `ansible/ansible`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_009", "prompt": "For `microsoft/semantic-kernel`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_010", "prompt": "For `PrefectHQ/prefect`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_011", "prompt": "For `kubernetes/kubernetes`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_012", "prompt": "For `redis/redis`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_013", "prompt": "For `openai/openai-python`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_014", "prompt": "For `psf/black`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_015", "prompt": "For `k3s-io/k3s`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_016", "prompt": "For `qdrant/qdrant`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_017", "prompt": "For `pnpm/pnpm`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_018", "prompt": "For `apache/superset`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_019", "prompt": "For `apache/airflow`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
{"id": "space_release_predecessor_020", "prompt": "For `fastapi/typer`, identify and verify the stable GitHub Release immediately preceding its most recent stable release published on or before 2026-08-24T23:59:59Z. Ignore drafts and prereleases. Return exactly one JSON object with keys `repo`, `tag`, and `published_date` (YYYY-MM-DD). Retain a first-party evidence URL in your prediction receipt.", "section": "exploratory_release_history_navigation", "task_type": "release_predecessor"}
