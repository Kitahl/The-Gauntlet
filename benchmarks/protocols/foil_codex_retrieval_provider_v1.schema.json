{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "status": {"type": "string", "enum": ["FOUND", "UNRESOLVED"]},
    "sources": {
      "type": "array",
      "maxItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "url": {"type": "string"},
          "title": {"type": "string"},
          "quote": {"type": "string"}
        },
        "required": ["url", "title", "quote"]
      }
    }
  },
  "required": ["status", "sources"]
}
