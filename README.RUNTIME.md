# Runtime extension status

The current feature branch adds an executable Process Assurance runtime and adaptive FOIL profile system. See `docs/RUNTIME_SETUP.md` for installation and use.

This file is temporary release documentation and may be folded into the main README after the feature passes CI.
